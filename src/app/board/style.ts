export const style = {
  display: 'grid',
  gridTemplateColumns: 'repeat(8, 1fr)', // Divide a largura em 8 colunas iguais
  gridTemplateRows: 'repeat(8, 1fr)',    // Divide a altura em 8 linhas iguais
  height: '100vh',
  
}